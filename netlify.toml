[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

[functions]
  included_files = ["drizzle/**"] # Если у тебя есть папка с миграциями, которую нужно включить